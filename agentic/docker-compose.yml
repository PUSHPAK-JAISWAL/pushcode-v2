services:
  agentic-server:
    build:
      context: .
    container_name: pushcode-agent
    ports:
      - "8081:8081"
    networks:
      - pushcode-network

networks:
  pushcode-network:
    external: true